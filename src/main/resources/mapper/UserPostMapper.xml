<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="spring.work.user.mapper.UserPostMapper">
    <insert id="saveUserPost"
            useGeneratedKeys="true"
            keyProperty="seq">
        insert into post (userId, title, content, view_cnt, created_user, updated_user)
        values (#{userId}, #{title}, #{content}, 0, #{userId}, #{userId})
    </insert>

    <update id="updateUserPost">
        update post
           set title = #{post.title}
             , content = #{post.content}
             , updated_at = now()
         where seq = #{postId}
           and userId = #{post.userId}
    </update>
</mapper>